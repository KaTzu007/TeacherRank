# TeacherRank
Веб-приложение для рейтинга и оценки преподавателей. Позволяет студентам регистрироваться, оставлять отзывы, ставить оценки и просматривать рейтинг преподавателей.

## 🛠 Технологический стек
* **Backend:** Python 3, Flask
* **Database:** PostgreSQL, SQLAlchemy (ORM)
* **Migrations:** Flask-Migrate (Alembic)
* **Auth:** Flask-Login, Flask-Bcrypt
* **Frontend:** HTML, CSS (Bootstrap), Jinja2 templates
* **Architecture:** Flask Blueprints (модульная структура)

## 🚀 Функционал
* **Авторизация:** Регистрация, вход, выход пользователей.
* **Система отзывов:** Возможность оставлять текстовые отзывы и звездный рейтинг.
* **Поиск:** Фильтрация по преподавателям и дисциплинам.



## ⚙️ Установка и запуск
Следуйте этим шагам, чтобы запустить проект локально.

1. Клонирование репозитория

```bash
git clone https://github.com/MY_USERNAME/TeacherRank.git
cd TeacherRank
```

2. Создание виртуального окружения
```Windows:
python -m venv venv
venv\Scripts\activate
```

3. Установка зависимостей
```
pip install -r requirements.txt
```

4. Настройка переменных окружения
В проекте используется файл .env для хранения секретных ключей. Создайте файл .env в корне проекта (на основе .env.example) и добавьте свои данные:
```
SECRET_KEY=ваш_секретный_ключ
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/teacherRank
```
Важно: Убедитесь, что у вас установлен PostgreSQL и создана база данных с именем teacherRank.

5. Применение миграций БД
Чтобы создать таблицы в базе данных:
```
flask db upgrade
```

6. Запуск приложения
```
python run.py
```



Структура проекта
Проект построен с использованием Blueprints для разделения логики:

blueprintapp/blueprints/auth — Авторизация и профили пользователей.

blueprintapp/blueprints/core — Главная страница и общая навигация.

blueprintapp/blueprints/reviews — Логика добавления и просмотра отзывов.

blueprintapp/blueprints/admin — Панель администратора.
